interface:
  display_name: "Conversation to Linear"
  short_description: "Sync clean, non-duplicate conversation outcomes into Linear"
  default_prompt: "Use $conversation-to-linear to extract only explicit, stack-realistic, monetization-relevant items from the current thread or an explicitly requested recent-history window. Dedupe against both Linear target projects, route code work to `samihalawa/2026-MANUS-oulang` and ideas to `Oulang Ideas & Inspirations`, move misfiled existing issues instead of duplicating them, and only create or extend genuinely useful issues."
